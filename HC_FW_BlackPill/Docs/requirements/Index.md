# Requirements Index

Current requirement areas:

- `host_controller/` - STM32 Host Controller firmware requirements, architecture,
  protocol, manager state machines, variables, and hardware test regime.

Retired Host Controller draft documents have been removed from the current
requirements tree. Use the Host Controller folder index for the current document
set.
